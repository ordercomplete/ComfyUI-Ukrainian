# Це патч-файл для виправлення помилки в comfyui_manager/legacy/manager_server.py
# 
# ПРОБЛЕМА:
# На рядку 787 викликається `await update_comfyui(None)`, але функція `update_comfyui`
# очікує `request: web.Request` і викликає `manager_security.reject_simple_form_post(request)`,
# що викликає помилку `AttributeError: 'NoneType' object has no attribute 'content_type'`
#
# РІШЕННЯ:
# Замінити рядок 787:
#     await update_comfyui(None)
# на:
#     temp_queue_batch.append(("update-comfyui", ('comfyui', core.get_config()['update_policy'] != 'nightly-comfyui')))
#
# Це те, що робить функція `update_comfyui` (рядок 1603-1604)

# Патч для рядка 786-787:
# БУЛО:
#         elif k == 'update_comfyui':
#             await update_comfyui(None)
#
# СТАЛО:
#         elif k == 'update_comfyui':
#             temp_queue_batch.append(("update-comfyui", ('comfyui', core.get_config()['update_policy'] != 'nightly-comfyui')))

# Щоб застосувати патч, відкрийте файл:
# D:\GEN\ComfyUI\python_embeded\Lib\site-packages\comfyui_manager\legacy\manager_server.py
#
# Знайдіть рядок 787 і замініть:
#     await update_comfyui(None)
# на:
#     temp_queue_batch.append(("update-comfyui", ('comfyui', core.get_config()['update_policy'] != 'nightly-comfyui')))

# ============================================================================
# ПАТЧ 2: Ін'єкція скрипта перекладу Manager V4.2.1 українською
# ============================================================================
# ПРОБЛЕМА:
# ComfyUI Manager V4.2.1 не має системи перекладу. Текст жорстко закодований в JS.
# Потрібно підключити скрипт перекладу до кожної HTML сторінки ComfyUI.
#
# РІШЕННЯ:
# Додати <script src="/comfyui-manager-locale/uk.js"></script> перед </head>
# в index.html ComfyUI frontend.
#
# ШЛЯХИ:
# - index.html: D:\ComfyUI-Ukrainian\comfyui_frontend_package\static\index.html
# - uk.js: custom_nodes\comfyui-manager-locale\web\manager-locale-loader.js
#
# Щоб застосувати патч:
# 1. Знайти </head> в index.html
# 2. Додати перед ним: <script src="/comfyui-manager-locale/uk.js"></script>
#
# ПРИМІТКА:
# Цей патч застосовується автоматично prestartup_script.py
# при кожному запуску ComfyUI.
# При оновленні ComfyUI index.html буде перезаписаний,
# але prestartup_script.py знову застосує патч.